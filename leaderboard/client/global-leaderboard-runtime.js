// === V6.1 GLOBAL LEADERBOARD ==============================================
const LEADERBOARD_API='https://voidcut-leaderboard.thiepn.workers.dev';
const LEADERBOARD_IDENTITY_KEY='voidcut.leaderboard.identity.v1';
const LEADERBOARD_IDENTITY_BACKUP_KEY='voidcut.leaderboard.identity.backup.v1';
const LEADERBOARD_IDENTITY_TEST_KEY='voidcut.leaderboard.identity.storage-test.v1';
const RANKED_START_WAIT_MS=2000;
const LEADERBOARD_SUBMISSION_QUEUE_KEY='voidcut.leaderboard.submissions.v1';
const LEADERBOARD_SUBMISSION_QUEUE_LIMIT=16;
const LEADERBOARD_QUEUE_RETRY_MS=15000;
let leaderboardTicket=null,leaderboardTicketPromise=null,activeLeaderboardTicket=null,leaderboardSubmissionQueue=[],leaderboardQueueDrainPromise=null,leaderboardQueueRetryTimer=null,leaderboardIdentityMemory=null;
function normalizeLeaderboardIdentity(x){if(!x||typeof x!=='object')return null;const playerId=String(x.playerId||'').trim(),name=String(x.name||'').trim().replace(/\s+/g,' '),token=String(x.token||'').trim();if(playerId.length<8||playerId.length>128||token.length<16||token.length>1024||!name||!/^[A-Za-z0-9 _-]{3,16}$/.test(name))return null;return{playerId,name,token}}
function leaderboardIdentityHash(text){let h=2166136261>>>0;for(let i=0;i<text.length;i++){h^=text.charCodeAt(i);h=Math.imul(h,16777619)>>>0}return h.toString(16).padStart(8,'0').toUpperCase()}
function leaderboardIdentityEnvelope(x,savedAt=Date.now()){const d=normalizeLeaderboardIdentity(x);if(!d)return null;const payload=JSON.stringify(d);return JSON.stringify({f:1,savedAt:Math.max(1,Math.floor(Number(savedAt)||Date.now())),h:leaderboardIdentityHash(payload),d})}
function parseLeaderboardIdentitySnapshot(raw){if(!raw)return null;try{const p=JSON.parse(raw);if(p&&p.f===1){const d=normalizeLeaderboardIdentity(p.d);if(!d||!Number.isFinite(p.savedAt)||p.savedAt<=0||typeof p.h!=='string'||leaderboardIdentityHash(JSON.stringify(d))!==p.h)return null;return{data:d,savedAt:Math.floor(p.savedAt),legacy:false}}const d=normalizeLeaderboardIdentity(p);return d?{data:d,savedAt:0,legacy:true}:null}catch{return null}}
function leaderboardIdentityStorageSelfTest(){const token=`${Date.now()}:${Math.random()}`;try{localStorage.setItem(LEADERBOARD_IDENTITY_TEST_KEY,token);const ok=localStorage.getItem(LEADERBOARD_IDENTITY_TEST_KEY)===token;localStorage.removeItem(LEADERBOARD_IDENTITY_TEST_KEY);return ok}catch{try{localStorage.removeItem(LEADERBOARD_IDENTITY_TEST_KEY)}catch{}return false}}
function sameLeaderboardIdentity(a,b){return!!a&&!!b&&a.playerId===b.playerId&&a.name===b.name&&a.token===b.token}
function writeLeaderboardIdentityCopy(key,envelope){try{localStorage.setItem(key,envelope);return localStorage.getItem(key)===envelope}catch{return false}}
function repairLeaderboardIdentityCopies(x,savedAt=Date.now()){const d=normalizeLeaderboardIdentity(x),env=d?leaderboardIdentityEnvelope(d,savedAt):null;if(!d||!env)return false;const backupOk=writeLeaderboardIdentityCopy(LEADERBOARD_IDENTITY_BACKUP_KEY,env),primaryOk=writeLeaderboardIdentityCopy(LEADERBOARD_IDENTITY_KEY,env),p=parseLeaderboardIdentitySnapshot(primaryOk?localStorage.getItem(LEADERBOARD_IDENTITY_KEY):null),b=parseLeaderboardIdentitySnapshot(backupOk?localStorage.getItem(LEADERBOARD_IDENTITY_BACKUP_KEY):null);return!!primaryOk&&!!backupOk&&sameLeaderboardIdentity(p?.data,d)&&sameLeaderboardIdentity(b?.data,d)}
function readStoredLeaderboardIdentity(){let primary=null,backup=null;try{primary=parseLeaderboardIdentitySnapshot(localStorage.getItem(LEADERBOARD_IDENTITY_KEY));backup=parseLeaderboardIdentitySnapshot(localStorage.getItem(LEADERBOARD_IDENTITY_BACKUP_KEY))}catch{return null}const candidates=[primary&&{...primary,priority:2},backup&&{...backup,priority:1}].filter(Boolean).sort((a,b)=>b.savedAt-a.savedAt||b.priority-a.priority);if(!candidates.length)return null;const chosen=candidates[0],needsRepair=chosen.legacy||!primary||!backup||!sameLeaderboardIdentity(primary.data,chosen.data)||!sameLeaderboardIdentity(backup.data,chosen.data);if(needsRepair)repairLeaderboardIdentityCopies(chosen.data,chosen.savedAt||Date.now());return chosen.data}
function loadLeaderboardIdentity(){const ident=readStoredLeaderboardIdentity();if(ident)leaderboardIdentityMemory=ident;return ident}
function storeLeaderboardIdentity(x){const d=normalizeLeaderboardIdentity(x);if(!d)return false;leaderboardIdentityMemory=d;if(!leaderboardIdentityStorageSelfTest())return false;const savedAt=Date.now();if(!repairLeaderboardIdentityCopies(d,savedAt))return false;const loaded=readStoredLeaderboardIdentity();return sameLeaderboardIdentity(loaded,d)}
function clearLeaderboardIdentity(){leaderboardIdentityMemory=null;for(const key of [LEADERBOARD_IDENTITY_KEY,LEADERBOARD_IDENTITY_BACKUP_KEY,LEADERBOARD_IDENTITY_TEST_KEY])try{localStorage.removeItem(key)}catch{}}
async function leaderboardRequest(path,options={},useAuth=true){const headers=new Headers(options.headers||{});if(options.body&&!headers.has('Content-Type'))headers.set('Content-Type','application/json');const ident=useAuth?loadLeaderboardIdentity():null;if(ident?.token)headers.set('Authorization',`Bearer ${ident.token}`);const ctrl=new AbortController(),timer=setTimeout(()=>ctrl.abort(),7000);try{const res=await fetch(`${LEADERBOARD_API}${path}`,{...options,headers,signal:ctrl.signal}),data=await res.json().catch(()=>({}));if(!res.ok){const err=new Error(data?.message||`Leaderboard request failed (${res.status})`);err.code=data?.error||'request-failed';err.status=res.status;throw err}return data}finally{clearTimeout(timer)}}
async function prefetchLeaderboardTicket(){if(leaderboardTicket&&leaderboardTicket.expiresAt>Date.now()+60000)return leaderboardTicket;if(leaderboardTicketPromise)return leaderboardTicketPromise;leaderboardTicketPromise=(async()=>{try{const data=await leaderboardRequest('/run/start',{method:'POST',body:'{}'},true);if(data?.ticketId&&Number.isInteger(data.seed)&&data?.ruleset?.replay===9){leaderboardTicket={ticketId:data.ticketId,seed:data.seed>>>0,expiresAt:Number(data.expiresAt)||0};return leaderboardTicket}}catch{}return null})().finally(()=>{leaderboardTicketPromise=null});return leaderboardTicketPromise}
function takeLeaderboardTicket(){if(!leaderboardTicket||leaderboardTicket.expiresAt<=Date.now()+1000){leaderboardTicket=null;return null}const t=leaderboardTicket;leaderboardTicket=null;return t}
async function acquireLeaderboardTicket(waitMs=RANKED_START_WAIT_MS){const ready=takeLeaderboardTicket();if(ready)return{ticket:ready,reason:null};const request=prefetchLeaderboardTicket();let timer=null;const outcome=await Promise.race([request.then(()=>'settled'),new Promise(resolve=>{timer=setTimeout(()=>resolve('timeout'),Math.max(0,Number(waitMs)||0))})]);if(timer!==null)clearTimeout(timer);const ticket=takeLeaderboardTicket();if(ticket)return{ticket,reason:null};return{ticket:null,reason:outcome==='timeout'?'LEADERBOARD TIMEOUT':'LEADERBOARD UNAVAILABLE'}}
function normalizeLeaderboardSubmissionQueue(raw,now=Date.now()){if(!Array.isArray(raw))return[];const out=[],seen=new Set;for(const item of raw){const ticket=item?.ticket,replay=item?.replay,id=String(item?.id||ticket?.ticketId||''),expiresAt=Number(ticket?.expiresAt)||0;if(!id||seen.has(id)||!ticket||String(ticket.ticketId||'')!==id||!replay||typeof replay!=='object'||!Number.isFinite(expiresAt)||expiresAt<=now+1000)continue;seen.add(id);out.push({id,ticket,replay,queuedAt:Number(item?.queuedAt)||now})}return out.slice(-LEADERBOARD_SUBMISSION_QUEUE_LIMIT)}
function loadLeaderboardSubmissionQueue(){try{return normalizeLeaderboardSubmissionQueue(JSON.parse(localStorage.getItem(LEADERBOARD_SUBMISSION_QUEUE_KEY)||'[]'))}catch{try{localStorage.removeItem(LEADERBOARD_SUBMISSION_QUEUE_KEY)}catch{}return[]}}
function persistLeaderboardSubmissionQueue(queue=leaderboardSubmissionQueue){const clean=normalizeLeaderboardSubmissionQueue(queue);leaderboardSubmissionQueue=clean;try{const text=JSON.stringify(clean);localStorage.setItem(LEADERBOARD_SUBMISSION_QUEUE_KEY,text);return localStorage.getItem(LEADERBOARD_SUBMISSION_QUEUE_KEY)===text}catch{return false}}
function enqueueLeaderboardSubmission(replay,ticket){const id=String(ticket?.ticketId||''),expiresAt=Number(ticket?.expiresAt)||0;if(!id||!replay||typeof replay!=='object'||!Number.isFinite(expiresAt)||expiresAt<=Date.now()+1000)return'invalid';const base=normalizeLeaderboardSubmissionQueue(leaderboardSubmissionQueue).filter(x=>x.id!==id);if(base.length>=LEADERBOARD_SUBMISSION_QUEUE_LIMIT)return'full';return persistLeaderboardSubmissionQueue([...base,{id,ticket,replay,queuedAt:Date.now()}])?'queued':'volatile'}
function removeLeaderboardSubmission(id){leaderboardSubmissionQueue=leaderboardSubmissionQueue.filter(x=>x.id!==id);const ok=persistLeaderboardSubmissionQueue(leaderboardSubmissionQueue);if(!leaderboardSubmissionQueue.length&&leaderboardQueueRetryTimer!==null){clearTimeout(leaderboardQueueRetryTimer);leaderboardQueueRetryTimer=null}return ok}
function terminalLeaderboardSubmissionError(err){return[400,403,404,409,410,413,422].includes(Number(err?.status)||0)}
function scheduleLeaderboardQueueRetry(){if(leaderboardQueueRetryTimer!==null||!leaderboardSubmissionQueue.length||!loadLeaderboardIdentity()||navigator.onLine===false)return;leaderboardQueueRetryTimer=setTimeout(()=>{leaderboardQueueRetryTimer=null;void drainLeaderboardSubmissionQueue()},LEADERBOARD_QUEUE_RETRY_MS)}
async function submitLeaderboardQueueEntry(entry){return leaderboardRequest(`/run/submit/${encodeURIComponent(entry.ticket.ticketId)}`,{method:'POST',body:JSON.stringify(entry.replay)},true)}
async function drainLeaderboardSubmissionQueue(){if(leaderboardQueueDrainPromise)return leaderboardQueueDrainPromise;leaderboardSubmissionQueue=normalizeLeaderboardSubmissionQueue(leaderboardSubmissionQueue);persistLeaderboardSubmissionQueue(leaderboardSubmissionQueue);if(!leaderboardSubmissionQueue.length)return null;if(!loadLeaderboardIdentity()){showLeaderboardJoin(`${leaderboardSubmissionQueue.length} ranked run${leaderboardSubmissionQueue.length===1?' is':'s are'} safely queued. Choose a public name to submit ${leaderboardSubmissionQueue.length===1?'it':'them'}.`);return null}leaderboardQueueDrainPromise=(async()=>{while(leaderboardSubmissionQueue.length){const entry=leaderboardSubmissionQueue[0];if(Number(entry.ticket?.expiresAt)<=Date.now()+1000){removeLeaderboardSubmission(entry.id);appendLeaderboardResult('GLOBAL QUEUED RUN EXPIRED');continue}try{appendLeaderboardResult('GLOBAL VERIFYING');const data=await submitLeaderboardQueueEntry(entry);removeLeaderboardSubmission(entry.id);ui.newBest.textContent=(ui.newBest.textContent||'').replace(/(?: • )?GLOBAL VERIFYING/g,'');hideLeaderboardJoin();const rank=data?.rank?`GLOBAL #${Number(data.rank).toLocaleString()}`:'GLOBAL VERIFIED';appendLeaderboardResult(data?.personalBest?`${rank} • PERSONAL BEST`:rank)}catch(err){ui.newBest.textContent=(ui.newBest.textContent||'').replace(/(?: • )?GLOBAL VERIFYING/g,'');if(err?.status===401){clearLeaderboardIdentity();showLeaderboardJoin(`${leaderboardSubmissionQueue.length} queued ranked run${leaderboardSubmissionQueue.length===1?' needs':'s need'} a valid leaderboard identity before submission.`);break}if(terminalLeaderboardSubmissionError(err)){removeLeaderboardSubmission(entry.id);appendLeaderboardResult('GLOBAL QUEUED RUN REJECTED');continue}appendLeaderboardResult('GLOBAL SUBMISSION QUEUED');scheduleLeaderboardQueueRetry();break}}if(!leaderboardSubmissionQueue.length)hideLeaderboardJoin()})().finally(()=>{leaderboardQueueDrainPromise=null});return leaderboardQueueDrainPromise}
function queueLeaderboardSubmission(replay,ticket){if(!replay||!ticket)return false;const result=enqueueLeaderboardSubmission(replay,ticket);if(result==='invalid')return false;if(result==='full'){appendLeaderboardResult('GLOBAL QUEUE FULL');return false}if(result==='volatile')appendLeaderboardResult('GLOBAL QUEUE STORAGE FAILED');if(loadLeaderboardIdentity())void drainLeaderboardSubmissionQueue();else showLeaderboardJoin(`${leaderboardSubmissionQueue.length} ranked run${leaderboardSubmissionQueue.length===1?' is':'s are'} queued. Choose one public name to submit ${leaderboardSubmissionQueue.length===1?'it':'them'}.`);return result==='queued'}
leaderboardSubmissionQueue=loadLeaderboardSubmissionQueue();
window.addEventListener('online',()=>void drainLeaderboardSubmissionQueue());
function leaderboardJoinCard(){let card=$('leaderboardJoinCard');if(card)return card;card=document.createElement('div');card.id='leaderboardJoinCard';card.className='leaderboard-join hidden';card.innerHTML='<div class="leaderboard-join-copy"><strong>JOIN GLOBAL LEADERBOARD</strong><span>Choose one public name. No email or password.</span></div><div class="leaderboard-join-form"><input id="leaderboardNameInput" maxlength="16" autocomplete="nickname" spellcheck="false" placeholder="PLAYER NAME" aria-label="Leaderboard name"><button id="leaderboardJoinButton" class="primary">JOIN</button></div><div id="leaderboardJoinStatus" class="leaderboard-join-status"></div>';ui.newBest.insertAdjacentElement('afterend',card);$('leaderboardJoinButton').addEventListener('click',()=>void createLeaderboardProfileFromCard());$('leaderboardNameInput').addEventListener('keydown',e=>{if(e.key==='Enter')void createLeaderboardProfileFromCard()});return card}
function showLeaderboardJoin(message='Name this profile once to publish your verified score.'){const card=leaderboardJoinCard();card.classList.remove('hidden');$('leaderboardJoinStatus').textContent=message;setTimeout(()=>$('leaderboardNameInput')?.focus({preventScroll:true}),50)}
function hideLeaderboardJoin(){const card=$('leaderboardJoinCard');if(card)card.classList.add('hidden')}
function appendLeaderboardResult(text){if(!text)return;const current=(ui.newBest.textContent||'').trim();if(!current.includes(text))ui.newBest.textContent=[current,text].filter(Boolean).join(' • ')}
async function createLeaderboardProfileFromCard(){const input=$('leaderboardNameInput'),button=$('leaderboardJoinButton'),status=$('leaderboardJoinStatus'),name=(input?.value||'').trim().replace(/\s+/g,' ');if(!/^[A-Za-z0-9 _-]{3,16}$/.test(name)){status.textContent='Use 3–16 letters, numbers, spaces, _ or -.';return}if(!leaderboardIdentityStorageSelfTest()){status.textContent='LOCAL STORAGE UNAVAILABLE • PROFILE NOT CREATED';return}button.disabled=true;status.textContent='CREATING PROFILE…';try{const data=await leaderboardRequest('/profile/create',{method:'POST',body:JSON.stringify({name})},false);if(!data?.player?.id||!data?.token)throw new Error('Invalid profile response');const ident=normalizeLeaderboardIdentity({playerId:data.player.id,name:data.player.name,token:data.token});if(!ident)throw new Error('Invalid profile response');if(!storeLeaderboardIdentity(ident)){leaderboardIdentityMemory=ident;status.textContent='PROFILE CREATED • IDENTITY STORAGE FAILED • EXPORT SAVE BEFORE CLOSING';return}status.textContent=leaderboardSubmissionQueue.length?`PROFILE READY • VERIFYING ${leaderboardSubmissionQueue.length} QUEUED RUN${leaderboardSubmissionQueue.length===1?'':'S'}…`:'PROFILE READY';await drainLeaderboardSubmissionQueue()}catch(err){status.textContent=err?.code==='name-taken'?'NAME ALREADY TAKEN':String(err?.message||'PROFILE CREATION FAILED').toUpperCase()}finally{button.disabled=false}}
async function watchGlobalReplay(hash){if(!hash)return;try{const replay=await leaderboardRequest(`/replay/${encodeURIComponent(hash)}`,{},false);if(validReplay(replay))startReplay(replay);else showCoach('REPLAY UNAVAILABLE','The leaderboard replay did not validate locally.','',1200)}catch{showCoach('REPLAY UNAVAILABLE','Could not load that verified run.','',1200)}}
async function loadGlobalLeaderboard(){const board=$('competitionBoard'),ident=loadLeaderboardIdentity();try{const q=ident?.playerId?`?limit=100&player_id=${encodeURIComponent(ident.playerId)}`:'?limit=100',data=await leaderboardRequest(`/leaderboard${q}`,{},false),rows=data?.rows||[];board.innerHTML='<div class="competition-head global-head"><span>RANK</span><span>PLAYER</span><span>SCORE</span><span>CHAMBER</span></div>'+(rows.length?rows.map(r=>`<button class="competition-row leaderboard-row${ident?.playerId===r.id?' is-self':''}" data-replay-hash="${escapeHtml(r.replayHash||'')}"><span class="competition-rank">${String(r.rank).padStart(2,'0')}</span><span class="leaderboard-name">${escapeHtml(r.name)}</span><span class="competition-score">${Number(r.score||0).toLocaleString()}</span><span class="competition-ch">${Number(r.chamber||1)}</span></button>`).join(''):'<div class="competition-row competition-empty"><span>NO VERIFIED GLOBAL SCORES YET</span></div>');board.querySelectorAll('[data-replay-hash]').forEach(row=>row.addEventListener('click',()=>void watchGlobalReplay(row.dataset.replayHash)));if(data?.self?.rank)$('competitionNote').textContent=`YOUR GLOBAL RANK #${Number(data.self.rank).toLocaleString()} • ${Number(data.self.score||0).toLocaleString()} POINTS • CHAMBER ${data.self.chamber}`;else if(ident)$('competitionNote').textContent=`${ident.name} • UNRANKED • Complete a server-ticketed PLAY run to enter the board.`;else $('competitionNote').textContent='Play normally. After your first ranked run, choose one public name to publish it — no email or password.'}catch{$('competitionNote').textContent='GLOBAL LEADERBOARD UNAVAILABLE • Normal PLAY remains fully available offline.';board.innerHTML='<div class="competition-row competition-empty"><span>COULD NOT REACH GLOBAL LEADERBOARD</span></div>'}}
