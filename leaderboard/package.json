{
  "name": "voidcut-leaderboard",
  "private": true,
  "type": "module",
  "scripts": {
    "build:verifier": "node scripts/build-verifier.mjs ../index.html",
    "check": "node --check src/index.js && node --check scripts/build-verifier.mjs",
    "test:adversarial": "npm run build:verifier && node scripts/test-adversarial-leaderboard.mjs"
  },
  "dependencies": {
    "acorn": "^8.15.0",
    "acorn-globals": "^7.0.1"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
